const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const notifications = await prisma.notification.findMany({ where: { employee_id: 4 }, orderBy: { created_at: 'desc' }, take: 20 }); const unreadCount = notifications.filter(n => !n.is_read).length; console.log('OK:', unreadCount); } catch (e) { console.error('ERROR:', e); } } main().finally(() => prisma.$disconnect());
